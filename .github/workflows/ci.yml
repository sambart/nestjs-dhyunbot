name: CI

on:
  push:
    branches: [develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint

      - name: Build shared
        run: pnpm --filter @dhyunbot/shared build

      - name: Build API
        run: pnpm --filter @dhyunbot/api build

      - name: Build Web
        run: pnpm --filter @dhyunbot/web build
